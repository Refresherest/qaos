# WO-117 — Opt-In Executive Factory

Baseline 1c344c7 on feat/operational-builder-chain. Authority:
OWNER-DECISION-023. Implement only keyword-only python_file_workspace opt-in
in executive/factory.py and focused factory tests, plus verification records.

Validate an explicit absolute existing output directory before graph creation;
register capability/skill/routes only in the chosen instance. Preserve omitted
behavior, state ownership, all session/CLI/planner contracts and global registries.
Verify focused/full tests, compile/import/architecture checks and unchanged active
data. Stop when the approved composition contract is verified.
