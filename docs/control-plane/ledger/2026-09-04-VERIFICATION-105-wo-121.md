# VERIFICATION-105 — WO-121

Outcome: ACCEPT WITH NOTES. Baseline 95b0295, 2026-09-04.

Command: `.venv\Scripts\python.exe docs/control-plane/ledger/wo121_public_api_probe.py`

PASS. Five fresh Python processes perform public-session build, CLI discovery,
another public-session build with the same target, public-session recovery of
the rejected attempt, and final CLI discovery. Probe exit codes: 0, 0, 1, 1, 0.
The 1 codes are intentionally emitted by the probe for caught FileExistsError;
they are not a new application CLI contract.

The source file is generated and verified, its Objective discovered completed.
The second Objective has a distinct ID and is failed. Reloaded recovery refuses
the existing target; Objective, Plan Task, QueueItem and queue action remain
coherently failed. Original successful Objective/Plan/Queue records and source
hash/timestamp are unchanged. Both listings preserve file bytes/timestamps.
Temporary state/output directories are removed; active data remains unchanged.

No product source, capability authority, providers, credentials or unrelated
changes modified. Probe and control-plane records only. The 223-test suite is
the WO-120 baseline, not rerun for this rehearsal. Same-agent Reviewer checklist
review, not independent delegation. This proves only the deterministic fixture
and expected refusal behavior, not generalized app building or sandbox security.
