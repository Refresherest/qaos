# OWNER-DECISION-031 — Isolated Validation Feasibility

2026-09-04. Authority: Qaasim April, repository owner.
The owner selected WO-147 Option A: isolated generated-code validation as the
milestone direction, authorizing only its first read-only feasibility assessment.

Inventory relevant local runtime availability/versions, host prerequisites and
repository integration seams using narrowly scoped read-only checks. Do not start
stopped services, inspect secrets/unrelated data or contact paid accounts. Request
additional system permissions explicitly when necessary. No runtime is designated.

Deliver all WO-147 gate outputs: dated non-secret command evidence; proposed
input/output/evidence contract and existing-domain ownership map; platform
recommendation or explicit no-feasible-option result; prerequisites and cost/
permission questions; independently authored acceptance/negative-test plan;
fail-closed stop/cleanup policy with no direct-host fallback.

Assess controls for network denial, credential exclusion, no repository/home
mounts, read-only inputs, disposable writes, process-tree termination, CPU/memory/
time/process/output/storage limits and reproducible cleanup. Distinguish proposed
controls from proven guarantees. Plan harmless smoke first and bounded reviewed
negative probes only after separate isolation/setup approval.

Not authorized: installation, downloads, service startup, VM/cloud provisioning,
paid account use/spend, network policy changes, runner implementation, untrusted
or isolation-probe execution. Each requires separate scoped authorization.
No GPU requirement, model designation, candidate generation or Content OS scope
is introduced. Preserve existing trusted builder, permissions, data and unrelated
work. This approval does not claim that an isolated runtime is ready or secure.
