# VERIFICATION-123 — WO-172 Restricted Worker Transport

2026-09-06; implementation baseline `cd90d90`; branch
`feat/operational-builder-chain`.

## Result

`ACCEPT WITH NOTES`. WO-172 installs the authenticated, forced-command restricted
worker transport authorized by OWNER-DECISION-035 and proves its boundary using
synthetic bytes and the fixed `harmless` launcher fixture only.

## Verification evidence

| Check | Result |
| --- | --- |
| Focused protocol, broker and controller tests | 71 passed, 1 intentional Windows `flock` skip |
| Full regression suite | 533 passed, 1 skip |
| Python compile check | passed locally and for installed worker files |
| Restricted synthetic exchange | completed |
| Replay/schema/path/hash negative suite | all rejected as required |
| Installed broker SHA-256 | `d0432efc08309eaa41e9e09e741c43da22582dcf6ab43e18796fbfe6588c157d` |
| Installed protocol SHA-256 | `5b1357f3e79c4ea6e3519c7e265b2ba8c7301750ef0530f7f538bd5b86b5c79c` |
| Forced key and sudoers | OpenSSH `restrict` plus exact broker command; sudoers parsed |
| Cleanup | owned staging and negative-container checks empty |
| Administrative continuity | fresh strict-host-key SSH succeeded |
| Rollback | armed before mutation; cancelled only after all live gates passed |

The first focused rerun encountered only the machine's inaccessible shared pytest
temporary directory. The isolated repository-owned rerun passed. An initially
generated transport key carried a literal two-quote passphrase due to Windows
native-argument handling; a non-disclosing check identified it, the passphrase was
removed under a temporary narrow ACL, and the unchanged public identity then
authenticated successfully.

Pre-correlation negative checks require remote-command exit status `1` and empty
stdout. SSH transport/authentication failure status `255` is explicitly not
accepted as broker-rejection evidence.

## Independent review

The QAOS Reviewer returned `ACCEPT WITH NOTES` with no blocker, major or minor
findings. The non-blocking future-hardening note is that the controller accepts any
syntactically valid `spec_sha256` reported for the authenticated, digest-pinned
`harmless` fixture instead of pinning one expected spec digest. This does not widen
the synthetic-only checkpoint and must be resolved before broader workload
admission is considered.

## Scope and stop condition

No generated code, QAOS source, real acceptance bundle, model output, provider
designation, new listener, arbitrary command path, or administrative access was
introduced. The transport remains verified for synthetic use only. Stop for a new
owner decision before any real candidate or QAOS-source transfer.
