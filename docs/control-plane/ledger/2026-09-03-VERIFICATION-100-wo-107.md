# VERIFICATION-100 — WO-107

Result: PASS on baseline 10fb771, 2026-09-03.

Reproduce from repository root:
`.venv\Scripts\python.exe docs/control-plane/ledger/wo107_operator_probe.py`

The initial CLI handler runs in-process with a test-only failure injected into
the second Task completion. Discovery, recovery and rediscovery each run in a
fresh Python process against the same disposable workspace.

- Exit codes: failure 1, discovery 0, recovery 0, rediscovery 0.
- CLI-reported identity matches persisted identity throughout.
- Discovery reports failed; rediscovery reports completed.
- Previously completed task record remains identical; all queue items complete.
- Both listings preserve workspace file contents and modification timestamps.
- Private injected exception text is absent from CLI output.
- Temporary workspace removed; active data contents and timestamps unchanged.

Scope review: probe and control-plane records only. No product code, provider,
credentials, model mappings or unrelated working-tree changes modified. Review
was performed by the executing agent using the Reviewer checklist, not an
independent delegated agent. This proves the bounded deterministic local flow,
not production workload readiness or OpenHands runtime recovery.

The 182-test regression result belongs to WO-106; it was not rerun for this
probe/documentation-only work order.
