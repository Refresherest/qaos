# VERIFICATION-103 — WO-117

Outcome: ACCEPT WITH NOTES. Baseline 1c344c7, 2026-09-03.

Factory now accepts keyword-only python_file_workspace. None preserves the
previous graph. Supplied paths must be absolute existing directories (empty or
relative paths cannot implicitly select cwd). Validation precedes construction
of the enabled graph. Only private instance registries receive PythonFileCapability,
python-file skill, exact python_file routing and planning default. Ordinary
system behavior and state ownership remain unchanged.

Commands and results:

- `.venv\Scripts\python.exe -m pytest tests/test_executive_factory_opt_in.py tests/test_executive_factory.py -q -p no:cacheprovider --basetemp C:/Projects/qaos/.wo117-focused-tmp` — 8 passed.
- `.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider --basetemp C:/Projects/qaos/.wo117-full-tmp` — 213 passed in 5.19s.
- Active data hashes and modification timestamps — unchanged.
- `.venv\Scripts\python.exe -m compileall -q src tests` — passed.
- pkgutil QAOS import sweep — 188 modules imported.
- `.venv\Scripts\python.exe tools/architecture_inspect.py` — 190 Python files,
  existing draft-ADR-based duplicate/registry/singleton leads only.
- JSON parsing and `git diff --check` — passed.

Tests verify exact factory wiring, normal-task routing and execution, typed
Task skill selection, private output bindings, two-instance/default isolation,
unchanged global registries, invalid paths and no files created by construction.
Typed routing tests inspect the factory graph rather than claiming a new
application submission API. Existing capability tests remain in the full suite.

Only factory source, focused factory tests and control-plane records changed.
Session, CLI, planner, capability source, model/provider configuration, secrets
and unrelated working-tree changes remain untouched. Architecture-awareness
kept the option local to the existing factory instead of adding a parallel graph.

NOTE: same-agent review using the Reviewer checklist, not independent review.
Executable submission through OperationalSession is still unimplemented and
requires a separately approved contract. No broader execution authority added.
