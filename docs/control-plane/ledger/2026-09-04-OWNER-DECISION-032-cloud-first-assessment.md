# OWNER-DECISION-032 — Cloud-First Execution Direction

2026-09-04. Authority: repository owner requested cloud as the default for real
future work, identified the existing Oracle VM and approved read-only assessment.
The owner supplied SSH endpoint/user and verified the ED25519 host fingerprint.

Supersede the local-host preparation recommendation as the next execution route.
Keep the laptop for oversight; prioritize disposable cloud work with durable
source/evidence outside disposable workers. Existing Oracle/OmniRoute service
must be preserved. Read-only inventory is approved using the existing SSH key;
private-key contents must not be displayed, copied or committed.

No installation, daemon changes, container execution, provisioning, spend, model
calls or untrusted-code admission authorized. Cloud-first direction does not
designate ordinary Docker as a sufficient isolation boundary or establish free-tier
capacity for additional VMs. Worker design/setup requires separate approval.
